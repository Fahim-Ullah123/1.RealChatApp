# Public HTTPS URL of the API in production. Do not include a trailing slash.
# Example: VITE_SERVER_URL=https://api.example.com
VITE_SERVER_URL=

# Local backend address used only by Vite's HTTPS development proxy.
VITE_LOCAL_API_URL=http://localhost:3000
